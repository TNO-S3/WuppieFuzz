openapi: 3.1.0
info:
  title: WuppieFuzz Action smoke test
  version: 1.0.0
servers:
  - url: http://127.0.0.1:8080
paths:
  /get:
    get:
      responses:
        "200":
          description: JSON response
          content:
            application/json:
              schema: true
